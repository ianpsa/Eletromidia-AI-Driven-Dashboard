version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
  settings:
    staticcheck:
      checks:
        - "all"
        - "-SA1019"
        - "-ST1000"
        - "-ST1003"
